/**
 * @deprecated Import from `@/lib/api/endpoints` and `@/config/identity` directly.
 * Re-exports kept for backward compatibility.
 */
export { UI_API } from '@/lib/api/endpoints';
export { resolveUserId, IDENTITY_CONFIG } from '@/config/identity';
